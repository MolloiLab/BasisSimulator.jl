{
  "project": "BasisSimulator.jl 10x Speed Build Loop (v2 — GPU-First)",
  "version": 2,
  "goal": "Implement GPU-validated optimizations from discovery to achieve 10x speedup on simulate!(). All work on branch speed/fused-projection. All speedup claims backed by Metal GPU benchmarks.",
  "spec": "ralph_loops/speed-spec.md",
  "branch": "speed/fused-projection",
  "v1_lesson": "v1 build stories were based on CPU-only benchmarks. All 4 stories were marked 'done' with CPU speedup numbers but produced ZERO speedup on Metal GPU. v2 stories are created by the discovery loop ONLY after GPU validation.",
  "gpu_baseline": {
    "simulate_total_ms": 5925,
    "forward_proj_unfused_ms": 5546,
    "forward_proj_fused_ms": 19160,
    "note": "Baseline is UNFUSED path. Fused is 3.65x slower on GPU. Forward proj varies ±300ms run-to-run (GPU thermal)."
  },
  "progress": {},
  "stories": [
    {
      "id": "SPEED-BUILD-V2-001",
      "title": "Disable fused path as default — immediate 3.65× speedup",
      "status": "ready",
      "gpu_speedup_measured": 3.65,
      "gpu_benchmark_details": "Fused: 19.16s, Unfused: 5.25s on Metal M3 Max. Gammex 472 128^3, 256×32×360 sinogram, 234 energy bins.",
      "description": "The v1 fused kernel (siddon_fused_poly_project!) is 3.65× slower than the unfused sequential path on Metal GPU because 234 energy accumulators (936 bytes) cause massive register spilling. Change the default from fused=true to fused=false in _forward_project_poly!. Keep fused code for future use with tiled approach.",
      "requirements": [
        "Change default fused parameter to false in _forward_project_poly! signature",
        "Or better: detect n_energies > threshold (e.g., 32) and auto-select unfused",
        "Keep fused kernel code intact for future tiled fusion work",
        "Verify simulate!() output is identical (fused and unfused produce same results)"
      ],
      "acceptanceCriteria": [
        "simulate!() runs in <6.5s on Gammex 472 128^3 benchmark (was 19.3s with fused)",
        "Output sinogram matches reference within Float32 tolerance",
        "No API changes — workspace creation and simulate! calls remain the same"
      ],
      "files": [
        "src/projection/polychromatic.jl"
      ],
      "spec_ref": "Section 0.2"
    },
    {
      "id": "SPEED-BUILD-V2-002",
      "title": "Tiled energy fusion — 3.79× speedup on forward projection",
      "status": "ready",
      "gpu_speedup_measured": 3.79,
      "gpu_benchmark_details": "K=16: 97.45ms/tile × 15 tiles = 1462ms vs unfused 5546ms on Metal M3 Max. Gammex 472 128×128×15, 256×32×360 sinogram, 234 energy bins. Benchmark: ralph_loops/bench_speed_001.jl",
      "description": "Replace 234 sequential Siddon calls (each 22.85ms) with 15 tiled fused kernel calls (each 97.45ms at K=16). Each tile traces DDA through mask ONCE and accumulates 16 energy bins via μ_table lookup. Tiles add partial Beer-Lambert sums to I_transmitted buffer. Final -log applied after all tiles. Re-uses 95% of existing siddon_fused_poly_project! kernel.",
      "requirements": [
        "Create siddon_tiled_poly_project! function that takes tile_start::Int32 and Val(K)",
        "Modify kernel to offset μ_table reads: μ_tbl[mat, tile_start + e - 1]",
        "Modify kernel to offset bowtie reads: bt[bt_base + (tile_start+e-2)*ncnr]",
        "Kernel ADDS partial Beer-Lambert sum to I_buffer (not -log to sinogram)",
        "Pad μ_table and wη to 240 = 15×16 (zero-pad last 6 bins) for even tiling",
        "Only compile Val(16) — last tile uses zero-padded weights, no extra specialization",
        "Update _forward_project_poly! to use tiled loop instead of sequential energy loop",
        "Keep unfused path available as fallback (fused=false kwarg)"
      ],
      "acceptanceCriteria": [
        "Forward projection completes in <1.8s on Gammex 472 128^3 benchmark (3.0× minimum speedup, target 3.79×)",
        "Full simulate!() completes in <2.5s (was ~6s unfused)",
        "Output sinogram matches unfused reference within Float32 tolerance (max abs diff < 1e-5)",
        "No API changes — workspace creation and simulate! calls remain the same",
        "Bowtie spectral transmission handled correctly per-tile"
      ],
      "files": [
        "src/projection/siddon.jl",
        "src/projection/polychromatic.jl"
      ],
      "spec_ref": "Section 1.2, 1.3",
      "depends_on": "SPEED-BUILD-V2-001 (disable fused first, then replace with tiled)"
    }
  ]
}
